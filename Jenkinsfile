pipeline{
    agent any
    stages {
        stage("hello") {
            steps {
                echo "welcome to jenkins pipeline"
            }
        }
  
            stage("creatingtheshfile") {
            steps {
                echo "creating a Sh file to jenkins pipeline"
                              }
        }
    }
}
